export const meta = {
  title: "Grid",
  path: "/styles/layouts/grid",
  docsPath: "styles/layouts/grid",
  filePath: "styles/layouts/_grid"
}

import {
  PreviewGridBasic,
  PreviewGridGap,
  PreviewGridGapVertical,
  PreviewGridGapHorizontal,
  PreviewGridAlign,
  PreviewGridReverse,
  PreviewGridStretch
} from "~/components/previews/preview-grid"
import { PreviewVariablesSingle } from "~/components/previews/preview-variables"
import PreviewMixins from "~/components/previews/preview-mixins"
import PreviewSources from "~/components/previews/preview-sources"

# Grid

## Table of Contents

contents list

## Detail

グリッドを構成するレイアウト CSS。

## Basic

`.grid` の直下に `.column` を複数配置。`.column` の幅はユーティリティの [Flex](/styles/utilities/flex) を付与して設定します。

<PreviewGridBasic />

| Selector  | Description                                    |
| --------- | ---------------------------------------------- |
| `.grid`   | グリッドの起点となるラッパー。                 |
| `.column` | `.grid` 直下に配置する。グリッドを分割する箱。 |

## Gap

`.is-gap-(way)-(value)` 形式のモディファイアを `.grid` に付与すると溝を作れます。`(value)` を省略した場合は `md` と同等の値になります。`(value)` は [Meter](/configs/variables/meter) の値を引き継いでいます。

`.is-gap-(value)` では上下左右の溝を同時設定します。

<PreviewGridGap />

| XXS           | XS           | Small        | Medium               | Large        | XL           | XXL           |
| ------------- | ------------ | ------------ | -------------------- | ------------ | ------------ | ------------- |
| `.is-gap-xxs` | `.is-gap-xs` | `.is-gap-sm` | `is-gap, .is-gap-md` | `.is-gap-lg` | `.is-gap-xl` | `.is-gap-xxl` |

### Vertical

`.is-gap-vertical-(value)` では上下方向のみ溝を設定します。Horizontal と同時利用が可能。

<PreviewGridGapVertical />

| XXS                    | XS                    | Small                 | Medium                                 | Large                 | XL                    | XXL                    |
| ---------------------- | --------------------- | --------------------- | -------------------------------------- | --------------------- | --------------------- | ---------------------- |
| `.is-gap-vertical-xxs` | `.is-gap-vertical-xs` | `.is-gap-vertical-sm` | `is-gap-vertical, .is-gap-vertical-md` | `.is-gap-vertical-lg` | `.is-gap-vertical-xl` | `.is-gap-vertical-xxl` |

### Horizontal

`.is-gap-horizontal-(value)` では左右方向のみ溝を設定します。Vertical と同時利用が可能。

<PreviewGridGapHorizontal />

| XXS                      | XS                      | Small                   | Medium                                     | Large                   | XL                      | XXL                      |
| ------------------------ | ----------------------- | ----------------------- | ------------------------------------------ | ----------------------- | ----------------------- | ------------------------ |
| `.is-gap-horizontal-xxs` | `.is-gap-horizontal-xs` | `.is-gap-horizontal-sm` | `is-gap-horizontal, .is-gap-horizontal-md` | `.is-gap-horizontal-lg` | `.is-gap-horizontal-xl` | `.is-gap-horizontal-xxl` |

## Align

<PreviewGridAlign />

| Modifier       | Description                                                       |
| -------------- | ----------------------------------------------------------------- |
| `.is-baseline` | `.grid` に付与すると `.column` がテキストベースラインで並びます。 |
| `.is-middle`   | `.grid` に付与すると `.column` が天地中央寄せで並びます。         |
| `.is-bottom`   | `.grid` に付与すると `.column` が下寄せで並びます。               |
| `.is-center`   | `.grid` に付与すると `.column` が左右中央寄せで並びます。         |
| `.is-right`    | `.grid` に付与すると `.column` が右寄せで並びます。               |
| `.is-between`  | `.grid` に付与すると `.column` が等間隔で並びます。               |
| `.is-around`   | `.grid` に付与すると `.column` が余白を含めた等間隔で並びます。   |

## Reverse

<PreviewGridReverse />

| Modifier      | Description                                                     |
| ------------- | --------------------------------------------------------------- |
| `.is-reverse` | `.grid` に付与すると `.column` が折り返した時に右から並びます。 |

## Stretch

<PreviewGridStretch />

| Modifier      | Description                                                            |
| ------------- | ---------------------------------------------------------------------- |
| `.is-stretch` | `.grid` に付与すると `.column` が Flexbox となり直下の高さが揃います。 |

## Variables

<PreviewVariablesSingle filePath={meta.filePath} viewCompiled={true} />

## Mixins

<PreviewMixins filePath={meta.filePath} />

## Sources

<PreviewSources
  title={meta.title}
  docsPath={meta.docsPath}
  scssPath={meta.filePath}
/>
