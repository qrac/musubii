export const meta = {
  title: "Grid",
  path: "/styles/layouts/grid",
  docsPath: "styles/layouts/grid",
  filePath: "styles/layouts/_grid"
}

import { PreviewGridBasic } from "~/components/previews/preview-grid"
import { PreviewVariablesSingle } from "~/components/previews/preview-variables"
import PreviewMixins from "~/components/previews/preview-mixins"
import PreviewSources from "~/components/previews/preview-sources"

# Grid

## Table of Contents

contents list

## Grid

グリッドレイアウトを作るための CSS。`.grid` の直下に `.column` を複数配置します。`.column` の幅はユーティリティの [Flex](/styles/utilities/flex) を付与して設定。

## Basic

<PreviewGridBasic />

## Variables

<PreviewVariablesSingle filePath={meta.filePath} viewCompiled={true} />

## Mixins

<PreviewMixins filePath={meta.filePath} />

## Sources

<PreviewSources
  title={meta.title}
  docsPath={meta.docsPath}
  scssPath={meta.filePath}
/>
