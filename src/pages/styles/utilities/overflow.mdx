export const meta = {
  title: "Overflow",
  path: "/styles/utilities/overflow",
  docsPath: "styles/utilities/overflow",
  filePath: "styles/utilities/_overflow"
}

import { PreviewOverflowBasic } from "~/components/previews/preview-overflow"
import { PreviewVariablesSingle } from "~/components/previews/preview-variables"
import PreviewMixins from "~/components/previews/preview-mixins"
import PreviewSources from "~/components/previews/preview-sources"

# Overflow

## Table of Contents

contents list

## Detail

共通モディファイアで `overflow` 関連のプロパティを操作するユーティリティ CSS。

## Basic

`.is-overflow-(value)` の形式で使います。

<PreviewOverflowBasic />

| Modifier                | Description                                          |
| ----------------------- | ---------------------------------------------------- |
| `.is-overflow-hidden`   | 収まらない部分があった場合、非表示にする。           |
| `.is-overflow-scroll-x` | 収まらない部分があった場合に横スクロールする。       |
| `.is-overflow-scroll-y` | 収まらない部分があった場合に縦スクロールする。       |
| `.is-overflow-ellipsis` | 収まらない文字があった場合に三点リーダーで省略する。 |

## Important

[Option](/configs/variables/option#option-important-xxxx) の `$option-important-overflow` を `true` にすると、全プロパティに `!important` を付与できます。

## Variables

<PreviewVariablesSingle filePath={meta.filePath} viewCompiled={false} />

## Mixins

<PreviewMixins filePath={meta.filePath} />

## Sources

<PreviewSources
  title={meta.title}
  docsPath={meta.docsPath}
  scssPath={meta.filePath}
/>
