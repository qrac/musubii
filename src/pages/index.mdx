export const title = "MUSUBii"

import SiteLogo from "~/components/parts/site-logo"

<SiteLogo />

Change Theme! 👇

```js
module.exports = {
  plugins: [
    "gatsby-plugin-sass",
    {
      resolve: "gatsby-plugin-sass-resources",
      options: {
        resources: ["src/assets/scss/configs.scss"]
      }
    }
  ]
}
```
